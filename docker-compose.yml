version: '3.8'
services:
  postgres_db:
    image: postgres:15
    container_name: de_target_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: weather_db
    ports:
      - "5432:5432"